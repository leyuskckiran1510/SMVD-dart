import 'downloadGui.dart';

import 'package:flutter/material.dart';

void main() {
  //runApp(FilePicker(fileNameToSave: "awdawd",));
  runApp(const MyApp());

  // ignore: unused_local_variable
  var a = const MyApp();
}
